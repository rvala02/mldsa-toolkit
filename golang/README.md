Simple test harness against Go. No support for alternative key sizes
(only ML-DSA-44), needs to be placed in Go source directory for compilation
(as it uses the internal fips module implementation; there's no public API).
